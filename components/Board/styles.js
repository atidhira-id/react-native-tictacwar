import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  board: {
    marginTop: 20,
    backgroundColor: "#bbb",
    borderWidth: 1,
  },
  row: {
    flex: 1,
    flexDirection: "row",
  },
});

export default styles;
